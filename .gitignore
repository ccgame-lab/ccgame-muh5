# Build / dev outputs (Nuxt, Nitro, bundlers) — match at any depth
.output
.data
.nuxt
.nitro
.cache
dist
artifacts

# Dependencies
node_modules

# Logs
logs
*.log

# OS / editor cruft
.DS_Store
.fleet
.idea

# Local env files (never commit secrets)
.env
.env.*
!.env.example
